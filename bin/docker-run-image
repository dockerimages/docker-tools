#!/bin/bash
#############
# Uses docker-inspect-env-value command to get value of DOCKER_RUN env var and execute that
# Usage: docker-run-image dockerimages/imagename
docker-inspect-env-value $1 DOCKER_RUN | exec $(awk '{print}')
