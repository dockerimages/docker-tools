#!/bin/bash
# Do The Var Export Exposed, Volume, Env

# Do The File Export rootfs and create baseimage from it
docker export $1 | docker import - ${2}${3}


docker build -t ${2}$3 - <<DOCKERFILE
FROM ${2}$3
$ENV
$VOLUME 
$ENTRYPOINT
$CMD
DOCKERFILE
