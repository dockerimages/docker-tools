#!/bin/bash
# THIS_TMP = $(mktemp)
# TODO:
# Check if Container exists
# Options for restarting same container maybe needs discussion

# This will Take the Container with id and make a Image from it as backup and free the name
docker export $1 | docker import - ${1}:latest && docker rm -f $1 && echo "done it Saved the Container to docker image with name containerid:latest you can re run it from there at any time"
